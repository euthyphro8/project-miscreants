package 
{
	import flash.display3D.textures.Texture;
	/**
	 * ...
	 * @author Josh Hess
	 */
	public class PARTICLE_SPAWNER 
	{
		public function PARTICLE_SPAWNER() 
		{
			//var particle:Texture = Texture.fromColor(width:Number, height:Number, color:uint = 0xffffff, alpha:Number = 1.0, optimizeForRenderToTexture:Boolean = false, scale:Number = -1, format:String = bgra, forcePotTexture:Boolean = false);	
			//http://doc.starling-framework.org/v2.1/starling/textures/Texture.html#fromColor()
		}
		
	}

}